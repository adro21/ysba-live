{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cp package.railway.json package.json && rm -rf node_modules && npm ci --no-cache && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start-site",
    "healthcheckPath": "/api/status"
  }
}