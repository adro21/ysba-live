[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["cp package.railway.json package.json", "npm ci"]

[phases.build]  
cmds = ["npm run build"]

[start]
cmd = "npm run start-site"